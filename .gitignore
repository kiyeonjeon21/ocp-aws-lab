# 자격증명 / 시크릿
.env
secrets/*
!secrets/.gitkeep
.mcp.json

# 클러스터 상태 (metadata.json 포함 - 절대 커밋하지 말 것, 절대 지우지도 말 것)
clusters/*
!clusters/.gitkeep

# 로컬 바이너리
bin/

# SSH 키
*.pem
id_ocp_lab
id_ocp_lab.pub

# OS
.DS_Store

# 실행 기록의 임시 포인터 (기록 본문 docs/runlog/*.md 는 커밋합니다)
docs/runlog/.current
docs/runlog/*.started

# 발행 전 초안 (LinkedIn / 블로그). 레포에 넣지 않습니다.
writeup/
